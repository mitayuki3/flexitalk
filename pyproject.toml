[project]
name = "flexitalk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gradio>=6.14.0",
    "irodori-tts",
]

[tool.uv.sources]
irodori-tts = { git = "https://github.com/Aratako/Irodori-TTS", rev = "main" }
